<Window x:Class="VerticalTabsControlExample.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:units="clr-namespace:VerticalTabControl.UI.Units;assembly=VerticalTabControl"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
        <DataTemplate x:Key="TabItem">
            <TextBlock Text="{Binding ItemName}"/>
        </DataTemplate>
        <DataTemplate x:Key="TabContent">
            <StackPanel  VerticalAlignment="Top" HorizontalAlignment="Center">
                <TextBlock Text="{Binding ItemName}" FontSize="20" FontWeight="Bold"/>
                <TextBlock Text="{Binding ItemContent}"/>
            </StackPanel>
        </DataTemplate>
    </Window.Resources>
    <Grid>
        <units:VerticalTabControl TabItem="{StaticResource TabItem}" TabContent="{StaticResource TabContent}" ItemsSource="{Binding TabModels}"/>
    </Grid>
</Window>
